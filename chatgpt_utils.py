import logging

def generate_reply_text(reply_text):
    logging.info("\n=== generate_reply_text reply_text ===")
    logging.info(reply_text)
    text = "リプライありがとう！"
    logging.info(text)
    return text